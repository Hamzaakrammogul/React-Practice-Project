const Wrapper = (props) =>{
return props.children
}
//Wrapper component is something that we dont need to build ourselves instead it
//comes with the react which is <React.Fragment> 

export default Wrapper;